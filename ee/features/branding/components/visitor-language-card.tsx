type Props = {
  [key: string]: any;
};

// Self-hosted build: visitor-language selector card is not available;
// renders nothing so the dataroom branding page still loads.
export function VisitorLanguageCard(_props: Props) {
  return null;
}
