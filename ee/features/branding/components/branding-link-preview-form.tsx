type Props = {
  [key: string]: any;
};

// Self-hosted build: link preview (social metatag) branding form is not
// available; renders nothing so the branding page still loads.
export function BrandingLinkPreviewForm(_props: Props) {
  return null;
}
