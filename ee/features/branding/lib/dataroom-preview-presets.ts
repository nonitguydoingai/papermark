// Self-hosted build: demo preview dataset for the branding preview page is
// not available.
export function getDataroomPreviewDataset(): {
  documents: any[];
  folders: any[];
} {
  return { documents: [], folders: [] };
}
