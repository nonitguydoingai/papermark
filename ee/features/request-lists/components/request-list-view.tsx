type Props = {
  [key: string]: any;
};

// Self-hosted build: request-list (task list) management view is not
// available; renders nothing so the dataroom tasks page still loads.
export function RequestListView(_props: Props) {
  return null;
}
