type Props = {
  [key: string]: any;
};

// Self-hosted build: viewer-facing request-list button is not available;
// renders nothing so the dataroom viewer still loads.
export function RequestListButton(_props: Props) {
  return null;
}
