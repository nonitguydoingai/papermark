type Props = {
  [key: string]: any;
};

// Self-hosted build: request-list (task list) settings card is not
// available; renders nothing so the dataroom settings page still loads.
export function RequestListSettingsCard(_props: Props) {
  return null;
}
